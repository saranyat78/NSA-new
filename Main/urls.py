from django.urls import path, re_path


from . import views

urlpatterns = [
    # path("", views.startPage, name="startup"),
    # path("home/", views.homePage(), name="home"),
    # path(r'', views.AllTech.as_view()),
    # re_path(r'(?P<pk>\d+)', views.TechView.as_view()),

]